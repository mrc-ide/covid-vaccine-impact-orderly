#just download the files from the raw folder
# file.copy(
#   file.path(orderly_loc, "data", "raw", "owid.Rds")
# )
file.copy(
  file.path(orderly_loc, "data", "raw", "worldsf.Rds"),
  "worldsf.Rds"
)
